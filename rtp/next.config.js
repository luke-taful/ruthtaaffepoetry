/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/ruthtaaffepoetry",
  output: "export",  // <=== enables static exports
  reactStrictMode: true,
};

module.exports = nextConfig;
